{
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "${process.env.BACKEND_URL}/api/$1"
        },
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}